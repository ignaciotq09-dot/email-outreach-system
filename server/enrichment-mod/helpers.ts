export function extractIndustry(text: string): string | undefined { const industries = ['Technology', 'Software', 'SaaS', 'Healthcare', 'Finance', 'Fintech', 'E-commerce', 'Retail', 'Manufacturing', 'Consulting', 'Education', 'Real Estate', 'Marketing', 'Advertising', 'Media', 'Telecommunications', 'Energy', 'Automotive', 'Aerospace', 'Biotechnology', 'Pharmaceuticals']; for (const industry of industries) if (text.toLowerCase().includes(industry.toLowerCase())) return industry; return undefined; }
export function extractCompanySize(text: string): string | undefined { const patterns = [/(\d+[-–]\d+)\s*employees/i, /(\d+)\+?\s*employees/i, /(small|medium|large|enterprise)\s*(company|business)/i]; for (const pattern of patterns) { const match = text.match(pattern); if (match) return match[1]; } return undefined; }
export function extractRevenue(text: string): string | undefined { const patterns = [/\$(\d+(?:\.\d+)?)\s*(million|billion|M|B)/i, /revenue.*?\$(\d+(?:\.\d+)?)\s*(million|billion|M|B)/i]; for (const pattern of patterns) { const match = text.match(pattern); if (match) return `$${match[1]} ${match[2]}`; } return undefined; }
export function extractNews(text: string): string | undefined { return text.length > 200 ? text.substring(0, 200) + '...' : text; }
export function extractLocation(text: string): string | undefined { const patterns = [/(based in|located in|headquarters in)\s*([A-Z][a-z]+(?:\s+[A-Z][a-z]+)*)/i, /([A-Z][a-z]+,\s*[A-Z]{2})/]; for (const pattern of patterns) { const match = text.match(pattern); if (match) return match[match.length - 1]; } return undefined; }
export function mapEmployeeCount(count: number | string | undefined): string | undefined { if (!count) return undefined; const num = typeof count === 'string' ? parseInt(count) : count; if (num < 11) return '1-10'; if (num < 51) return '11-50'; if (num < 201) return '51-200'; if (num < 501) return '201-500'; if (num < 1001) return '501-1000'; if (num < 5001) return '1001-5000'; return '5000+'; }
